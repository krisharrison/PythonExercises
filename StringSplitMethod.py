# using space(or whatever character you choose) as a delimiter, the split method returns a list of separate strings from a single string

message = input(">")

words = message.split(" ")

print(words)